# Netlify Drop 使用指南

## 什么是 Netlify Drop？

Netlify Drop 是 Netlify 提供的最简单的静态网站部署方式，**无需配置、无需GitHub、无需命令行**，只需拖拽文件夹即可部署。

---

## 使用步骤

### 第一步：准备构建好的文件

```bash
# 在项目根目录运行
npm run build

# 确保 dist 文件夹生成成功
# dist 文件夹应该包含：
# - index.html
# - assets/ (JS和CSS文件)
# - mediapipe/ (AI模型文件)
# - heart.svg
# - _redirects
```

### 第二步：访问 Netlify Drop

1. 打开浏览器访问：**https://app.netlify.com/drop**
   
   或者直接访问 Netlify 官网，点击 "Sites" → "Add new site" → "Deploy manually"

### 第三步：拖拽部署

![拖拽示意图]

1. **找到你的 dist 文件夹**
   - 路径：`c:\Users\26404\.trae-cn\Project\画爱心\dist`

2. **直接拖拽**
   - 将 dist 文件夹从文件资源管理器拖拽到 Netlify Drop 页面
   - 或者点击 "Upload folder" 按钮选择 dist 文件夹

3. **等待上传**
   - 文件上传中...
   - 自动部署中...
   - 完成！

### 第四步：获取网站链接

部署完成后，Netlify 会自动生成一个链接：
```
https://xxx-xxx-xxx.netlify.app
```

- ✅ 支持 HTTPS
- ✅ 全球 CDN 加速
- ✅ 立即生效

---

## 完整操作流程图

```
┌─────────────────┐
│  1. npm run build │
│   构建项目       │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  2. 找到 dist    │
│   文件夹         │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  3. 访问         │
│  netlify.com/drop│
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  4. 拖拽 dist    │
│   文件夹到网页   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  5. 等待部署完成 │
│   获取网站链接   │
└─────────────────┘
```

---

## 常见问题

### Q1: 拖拽后显示 "Upload failed"?

**解决方法：**
- 检查 dist 文件夹是否存在
- 检查文件夹大小（不能超过 1GB）
- 尝试刷新页面重新拖拽

### Q2: 网站部署后显示 404?

**解决方法：**
- 确保 dist 文件夹包含 `index.html`
- 确保 `_redirects` 文件存在（已在 public 文件夹中）

### Q3: 如何更新网站？

**方法：**
1. 重新运行 `npm run build`
2. 再次访问 netlify.com/drop
3. 拖拽新的 dist 文件夹
4. 会自动部署到同一个链接

### Q4: 如何绑定自定义域名？

**步骤：**
1. 部署完成后进入 Site settings
2. 点击 "Domain management"
3. 点击 "Add custom domain"
4. 输入你的域名并按照提示配置 DNS

---

## 优势对比

| 特性 | Netlify Drop | Git 自动部署 |
|------|-------------|-------------|
| 难度 | ⭐ 超简单 | ⭐⭐⭐ 较复杂 |
| 速度 | ⚡ 立即部署 | ⏱️ 需要等待 |
| 适合场景 | 快速测试、演示 | 正式项目、团队协作 |
| 版本控制 | ❌ 无 | ✅ 有 |
| 自动更新 | ❌ 手动拖拽 | ✅ 自动部署 |

---

## 现在就开始

### 快速命令

```bash
# 1. 进入项目目录
cd "c:\Users\26404\.trae-cn\Project\画爱心"

# 2. 构建项目
npm run build

# 3. 确认 dist 文件夹存在
ls dist

# 4. 打开 Netlify Drop
start https://app.netlify.com/drop

# 5. 拖拽 dist 文件夹到浏览器
```

### 部署前检查清单

- [ ] 运行 `npm run build` 成功
- [ ] dist 文件夹存在
- [ ] dist/index.html 存在
- [ ] dist/assets/ 文件夹存在
- [ ] dist/mediapipe/ 文件夹存在
- [ ] dist/_redirects 文件存在
- [ ] dist/heart.svg 存在

---

## 视频演示

```
步骤演示：

1. 文件资源管理器          2. Netlify Drop 页面
   ┌─────────────┐         ┌─────────────┐
   │ 📁 dist     │  ──►    │   拖拽区域   │
   │ 📄 index.html│         │  ┌───────┐  │
   │ 📁 assets   │         │  │       │  │
   │ 📁 mediapipe│         │  │  +    │  │
   └─────────────┘         │  │       │  │
                           │  └───────┘  │
                           └─────────────┘

3. 拖拽后                  4. 部署完成
   ┌─────────────┐         ┌─────────────┐
   │ 上传中...   │  ──►    │ ✅ 部署成功  │
   │ ████████░░░ │         │              │
   └─────────────┘         │ 🌐 网站链接: │
                           │ xxx.netlify.app
                           └─────────────┘
```

---

## 需要帮助？

如果遇到问题：
1. 检查浏览器控制台 (F12) 的错误信息
2. 确认所有文件都在 dist 文件夹中
3. 尝试清除浏览器缓存后刷新
4. 重新拖拽部署一次

**最简单的方法：直接拖拽 dist 文件夹到 https://app.netlify.com/drop**
